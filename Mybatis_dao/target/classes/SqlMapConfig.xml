<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!--加载properties文件-->
    <properties resource="jdbc.properties"></properties>

    <!--设置别名-->
    <typeAliases>
        <!--方式一：给单个实体起别名-->
<!--        <typeAlias type="domain.com.gvc.User" alias="user"></typeAlias>-->
        <!--方式二：批量起别名, 别名就是类名，且不区分大小写-->
        <package name="com.gvc.domain"/>
    </typeAliases>

    <plugins>
        <plugin interceptor="com.github.pagehelper.PageHelper">
            <!-- dialect: 指定方言 “limit” -->
            <property name="dialect" value="mysql"/>
        </plugin>
    </plugins>
        
    <!--environments: 运行环境, default表示默认运行哪个环境-->
    <environments default="development">
        <!--数据库环境配置-->
        <environment id="development">
            <!--事务管理器,当前事务交由JDBC管理-->
            <transactionManager type="JDBC"></transactionManager>
            <!--数据源信息, POOLED表示使用类型为Mybatis连接池-->
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>

    <!--引入映射配置文件-->
    <mappers>
<!--        <mapper resource="com/gvc/mapper/UserMapper.xml"></mapper>-->

        <!--使用该方法，接口和映射文件需要同包同名-->
<!--        <mapper class="com.gvc.mapper.UserMapper"></mapper>-->

        <!--批量加载映射-->
        <package name="com.gvc.mapper"/>
    </mappers>

</configuration>